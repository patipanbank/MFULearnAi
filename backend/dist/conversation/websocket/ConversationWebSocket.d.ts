import { EventEmitter } from 'events';
import { WebSocketResponse } from '../types';
export declare class ConversationWebSocket extends EventEmitter {
    private wss;
    private connections;
    private rooms;
    private userConnections;
    private readonly HEARTBEAT_INTERVAL;
    private readonly CONNECTION_TIMEOUT;
    private readonly MAX_CONNECTIONS_PER_USER;
    private readonly MESSAGE_RATE_LIMIT;
    private heartbeatTimer;
    private cleanupTimer;
    constructor(server: any);
    private setupWebSocketServer;
    private handleConnection;
    private setupConnectionHandlers;
    private handleMessage;
    private routeMessage;
    private handleHeartbeat;
    private handleJoinConversation;
    private handleLeaveConversation;
    private handleSendMessage;
    private handleTypingStart;
    private handleTypingStop;
    private joinRoom;
    private leaveRoom;
    broadcastToRoom(conversationId: string, message: WebSocketResponse, excludeConnectionId?: string): void;
    sendToConnection(connectionId: string, message: WebSocketResponse): boolean;
    private startHeartbeat;
    private performHeartbeat;
    private handlePong;
    private startCleanup;
    private performCleanup;
    private verifyToken;
    private checkConnectionLimits;
    private checkRateLimit;
    private validateMessage;
    private getClientIP;
    private addUserConnection;
    private removeUserConnection;
    private sendError;
    private closeWithError;
    private handleConnectionError;
    private handleDisconnection;
    getStats(): any;
    getRoomStats(conversationId: string): any;
    shutdown(): Promise<void>;
}
//# sourceMappingURL=ConversationWebSocket.d.ts.map